// TODO: Add tests for filters
describe("Filters", () => {
  it("should render correctly", () => {
    // verify that the component renders correctly with different cases: empty filters, single filter, multiple filters
    // verify by querying texts or snapshots
  });

  it("should behave correctly", () => {
    // verify that the component behaves correctly when adding filter rule/group
    // verify that changing the filter does update UI and value
    // verify by simulating user interactions and checking the output texts
  });
});
