describe("Filter", () => {
  describe("RichTextFilter", () => {
    it("should render correctly", () => {});

    it("should behave correctly", () => {});
  });

  describe("MultiSelectFilter", () => {
    it("should render correctly", () => {
      // verify that the component renders correctly with: string operators and boolean operators
    });

    it("should behave correctly", () => {
      // verify that the component behaves correctly with: string operators and boolean operators
    });
  });
});
